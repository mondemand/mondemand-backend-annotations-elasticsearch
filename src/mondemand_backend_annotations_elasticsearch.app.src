{ application, mondemand_backend_annotations_elasticsearch,
  [
    { description,"A plugin for the mondemand server to send annotations to elasic search."},
    {vsn, {cmd, "/bin/bash -c 'awk \"match(\\$0, /[0-9]+\\.[0-9]+(\\.[0-9]+)+/){print substr(\\$0, RSTART,RLENGTH); exit}\" ChangeLog'"}},
    { modules,[]},
    { registered,[]},
    { applications, [kernel,stdlib]},
    { env, [] }
  ]
}.
